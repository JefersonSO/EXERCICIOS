algoritmo "maior numero da lista de 100 2.0"
// Faça um programa que leia 100 valores e no final, escreva o maior e o menor valor lido.
var
 maior, n, c: Inteiro
inicio
 c <- maior <- 0
 Repita
  c <- c + 1
  n <- randi(100))
  Escreval(n)
  Se n > maior entao
    maior <- n)

  FimSe
 Ate c = 100

  Escreva(" o maior numero é: ", maior )
    
fimalgoritmo
